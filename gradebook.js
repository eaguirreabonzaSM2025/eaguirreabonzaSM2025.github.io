function fetchGradeData() {
    console.log("fetchGradeData called");
    // This function will fetch data from the database in a future assignment
}

//Populate the table with grade data
function populateGradebook(data) {
    console.log("populateGradebook called with data:", data);
    // This function will populate the gradebook table with the provided data
}

// Call the stubs for testing (demonstrate workflow)
fetchGradeData();
populateGradebook([]);
